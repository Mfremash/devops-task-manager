apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  selector:
    app: laravel-backend
  ports:
    - protocol: TCP
      port: {{ .Values.backend.service.port }}
      targetPort: 8000
  type: ClusterIP
